{% extends 'base.html.twig' %}

{% block body %}
  <div class="row justify-content-center">
    <div class="col-md-10">
      <div class="list-group">
      {% for entreprise in entreprises%}
        <a href="{{path('prostage_resultatRecherche_entreprises',{id:entreprise.id})}}" class="list-group-item list-group-item-action"> {{entreprise.nom}} </a>
      {% endfor %}
      </div>
    </div>
  </div>
{% endblock %}
