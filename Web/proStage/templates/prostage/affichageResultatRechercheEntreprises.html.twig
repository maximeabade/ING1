{% extends 'base.html.twig' %}

{% block body %}
  <div class="row justify-content-center">
    <div class="col-md-10">
      <div class="list-group">
      {% for stage in entreprise.stages %}
        <a href="{{path('prostage_resultatRecherche_entreprises',{id:stage.id})}}" class="list-group-item list-group-item-action"> {{stage.nom}} </a>
      {% endfor %}
      </div>
    </div>
  </div>
{% endblock %}
